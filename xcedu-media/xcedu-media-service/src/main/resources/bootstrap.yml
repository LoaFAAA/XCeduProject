spring:
  application:
    name: media-service
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:
        namespace: xcproject
        group: xceduProject
      config:
        namespace: xcproject
        group: xceduProject
        file-extension: yaml
        refresh-enabled: true


#profiles默认为dev
  profiles:
    active: xcproject

logging:
  config: classpath:log4j2-dev.xml

