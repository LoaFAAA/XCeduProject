spring:
  application:
    name: system-service
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:
        namespace: xcproject
        group: xceduProject
      config:
        namespace: xcproject
        group: xceduProject
        file-extension: yaml
        refresh-enabled: true
  profiles:
    active: xcproject
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml


